#define CATCH_CONFIG_MAIN

#include "catch.hpp"
#include "foo.hpp"

// custom <custom includes>
// end <custom includes>

// custom <foo begin namespace>
// end <foo begin namespace>

SCENARIO("basics") {
// custom <(825963486)>
// end <(825963486)>
  GIVEN("a vector with some items") {
  // custom <(463197197)>
  // end <(463197197)>
    WHEN("the size is increased") {
    // custom <(693881657)>
    // end <(693881657)>
      THEN("the size and capacity change") {
      // custom <(31643468)>
      // end <(31643468)>
      }
    }
    WHEN("the size is reduced") {
    // custom <(398518674)>
    // end <(398518674)>
      THEN("the size channges but not capacity") {
      // custom <(150264450)>
      // end <(150264450)>
      }
    }
    WHEN("more capacity is reserved") {
    // custom <(130939743)>
    // end <(130939743)>
      THEN("the capacity changes but not the size") {
      // custom <(649869633)>
      // end <(649869633)>
      }
    }
    WHEN("less capacity is reserved") {
    // custom <(177474090)>
    // end <(177474090)>
      THEN("neither size nore capacity is changed") {
      // custom <(977535991)>
      // end <(977535991)>
      }
    }
  }
}
SCENARIO("basics") {
// custom <(825963486)>
// end <(825963486)>
  GIVEN("a vector with some items") {
  // custom <(463197197)>
  // end <(463197197)>
    WHEN("the size is increased") {
    // custom <(693881657)>
    // end <(693881657)>
      THEN("the size and capacity change") {
      // custom <(31643468)>
      // end <(31643468)>
      }
    }
    WHEN("the size is reduced") {
    // custom <(398518674)>
    // end <(398518674)>
      THEN("the size channges but not capacity") {
      // custom <(150264450)>
      // end <(150264450)>
      }
    }
    WHEN("more capacity is reserved") {
    // custom <(130939743)>
    // end <(130939743)>
      THEN("the capacity changes but not the size") {
      // custom <(649869633)>
      // end <(649869633)>
      }
    }
    WHEN("less capacity is reserved") {
    // custom <(177474090)>
    // end <(177474090)>
      THEN("neither size nore capacity is changed") {
      // custom <(977535991)>
      // end <(977535991)>
      }
    }
  }
}
SCENARIO("basics") {
// custom <(825963486)>
// end <(825963486)>
  GIVEN("a vector with some items") {
  // custom <(463197197)>
  // end <(463197197)>
    WHEN("the size is increased") {
    // custom <(693881657)>
    // end <(693881657)>
      THEN("the size and capacity change") {
      // custom <(31643468)>
      // end <(31643468)>
      }
    }
    WHEN("the size is reduced") {
    // custom <(398518674)>
    // end <(398518674)>
      THEN("the size channges but not capacity") {
      // custom <(150264450)>
      // end <(150264450)>
      }
    }
    WHEN("more capacity is reserved") {
    // custom <(130939743)>
    // end <(130939743)>
      THEN("the capacity changes but not the size") {
      // custom <(649869633)>
      // end <(649869633)>
      }
    }
    WHEN("less capacity is reserved") {
    // custom <(177474090)>
    // end <(177474090)>
      THEN("neither size nore capacity is changed") {
      // custom <(977535991)>
      // end <(977535991)>
      }
    }
  }
}

